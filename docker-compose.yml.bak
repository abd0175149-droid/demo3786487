# ============================================================
# DEMO — Docker Compose (Production)
# Port: 4010 → Cloudflare Tunnel → DEMO.grade.sbs
# ============================================================

services:
  demo-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: demo-app
    restart: unless-stopped
    ports:
      - "4010:4010"
    volumes:
      # Persist storage (uploads, logs, and sqlite database)
      - DEMO-storage:/var/www/html/storage
    environment:
      - APP_NAME=DEMO
      - APP_ENV=production
      - APP_DEBUG=false
      - APP_URL=https://DEMO.grade.sbs
      - APP_LOCALE=ar
      - LOG_CHANNEL=stack
      - LOG_LEVEL=error
      - DB_CONNECTION=sqlite
      - SESSION_DRIVER=database
      - QUEUE_CONNECTION=database
      - CACHE_STORE=database
      - SAR_TO_JOD=0.1880
    env_file:
      - .env.production
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:4010/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

volumes:
  DEMO-storage:
    driver: local
